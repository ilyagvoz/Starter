{
  "name": "start",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "bun run scripts/test-summary.ts",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "update-deps": "pnpm update -r"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.7.2"
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=18"
  }
}
